interface:
  display_name: "PII Data Safety"
  short_description: "Detect and reduce PII exposure risk"
  default_prompt: "Use $pii-data-safety to audit this project for PII handling and exposure risks."
